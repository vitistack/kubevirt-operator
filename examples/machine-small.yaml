---
apiVersion: vitistack.io/v1alpha1
kind: Machine
metadata:
  name: example-machine-small
spec:
  # Using predefined small machine class
  machineClass: "small"
  # No resource overrides - will use defaults from MachineClass
  name: "example-vm-small"
  provider: kubevirt
