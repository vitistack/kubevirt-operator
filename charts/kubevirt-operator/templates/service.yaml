apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubevirt-operator.fullname" . }}
  labels:
    {{- include "kubevirt-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubevirt-operator.selectorLabels" . | nindent 4 }}
